{
    "name": "chatview_connect",
    "version": "3.0.0",
    "description": "Flutter chatview wrapper",
    "scripts": {
        "build": "flutter build web --release",
        "vercel-build": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable $HOME/flutter && $HOME/flutter/bin/flutter build web --release"
    }
}