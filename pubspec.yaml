name: chatview_connect
description: "Specialized wrapper for chatview package providing seamless integration with local or custom cloud services for your flutter chat app."
version: 3.0.0
issue_tracker: https://github.com/theshubhamgundu/chatview/issues
homepage: https://github.com/theshubhamgundu/chatview
topics:
  - chatview
  - chat-backend
  - plug-and-play
  - backend-integration

platforms:
  android:
  ios:
  web:
  macos:
  windows:

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  chatview_utils: 3.0.0

  flutter:
    sdk: flutter

  http: ^1.4.0
  intl: ^0.20.2
  path: ^1.9.0
  rxdart: ^0.28.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
